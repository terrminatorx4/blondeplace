---
export interface Props {
  title: string;
  description?: string;
}

const { title, description } = Astro.props;
---

<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8" />
  <meta name="description" content={description || "BLONDE PLACE - салон красоты комфорт класса в Санкт-Петербурге. Профессиональное окрашивание волос, мелирование, сложное окрашивание."} />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
  <title>{title}</title>
  
  <!-- Google Fonts - exactly like blondeplace.ru -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;500;600;700&family=Inter:wght@300;400;500;600&display=swap" rel="stylesheet">
</head>
<body>
  <!-- EXACT COPY OF blondeplace.ru HEADER -->
  <header class="main-header">
    <div class="header-container">
      <!-- Logo - exactly like original -->
      <div class="logo">
        <div class="logo-text">BLONDEPLACE</div>
      </div>
      
      <!-- Right buttons - exactly like original -->
      <div class="header-buttons">
        <button class="header-btn">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor">
            <path d="M12 0C5.373 0 0 5.373 0 12s5.373 12 12 12 12-5.373 12-12S18.627 0 12 0zm0 22C6.486 22 2 17.514 2 12S6.486 2 12 2s10 4.486 10 10-4.486 10-10 10z"/>
            <path d="M12 6c-3.309 0-6 2.691-6 6s2.691 6 6 6 6-2.691 6-6-2.691-6-6-6zm0 10c-2.206 0-4-1.794-4-4s1.794-4 4-4 4 1.794 4 4-1.794 4-4 4z"/>
          </svg>
        </button>
        <button class="header-btn">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor">
            <path d="M20 6L9 17l-5-5"/>
          </svg>
        </button>
        <button class="header-menu-btn">
          <span></span>
          <span></span>
          <span></span>
        </button>
      </div>
    </div>
  </header>

  <main>
    <slot />
  </main>

  <!-- Footer with navigation (since header has no menu) -->
  <footer class="main-footer">
    <div class="footer-container">
      <div class="footer-sections">
        <!-- Logo section -->
        <div class="footer-section">
          <div class="footer-logo">
            <div class="footer-logo-text">BLONDE PLACE</div>
            <div class="footer-tagline">салон красоты комфорт класса</div>
          </div>
        </div>

        <!-- Navigation -->
        <div class="footer-section">
          <h3>Главная</h3>
          <ul>
            <li><a href="/">о нас</a></li>
            <li><a href="/services">наши услуги</a></li>
            <li><a href="/blog">получите скидку</a></li>
            <li><a href="/beauty-coworking">коворкинг</a></li>
            <li><a href="https://blondeplace.ru/#masters" target="_blank">наши мастера</a></li>
            <li><a href="/contacts">контакты</a></li>
            <li><a href="/blog">новостная лента</a></li>
          </ul>
        </div>

        <!-- Services -->
        <div class="footer-section">
          <h3>Услуги</h3>
          <ul>
            <li><a href="/services">мелирование с растяжкой цвета</a></li>
            <li><a href="/services">мелирование</a></li>
            <li><a href="/services">сложное окрашивание+стрижка</a></li>
            <li><a href="/services">контуринг</a></li>
            <li><a href="/services">окрашивание корней</a></li>
            <li><a href="/services">тотал блонд с нуля</a></li>
            <li><a href="/services">шатуш</a></li>
            <li><a href="/services">AirTouch ниже плеч</a></li>
            <li><a href="/services">тонирование</a></li>
            <li><a href="/services">тотал блонд (окрашенная база)</a></li>
          </ul>
        </div>

        <!-- Consultation -->
        <div class="footer-section">
          <h3>Консультация</h3>
          <ul>
            <li><a href="tel:+79819608722">Ваш телефон</a></li>
          </ul>
          <div class="phone-input-wrapper">
            <input type="tel" placeholder="+7 (999) 999-99-99" class="phone-input">
            <button class="phone-submit">▶</button>
          </div>
        </div>

        <!-- Contacts -->
        <div class="footer-section">
          <h3>Контакты</h3>
          <div class="contact-info">
            <p><strong>+7 (981) 960-87-22</strong></p>
            <p>г. Санкт-Петербург, Дегтярная, дом 9</p>
            <p class="metro-info">
              м. Площадь Восстания, 12 мин<br>
              м. Александра Невского, 19 мин<br>  
              м. Маяковского, 12 мин
            </p>
          </div>
        </div>
      </div>

      <div class="footer-bottom">
        <div class="footer-legal">
          <p>ИП РАФАЕНКО ОА<br>ОГРН 313715413300153</p>
        </div>
        <div class="footer-copyright">
          <p>© 2025 BLONDE PLACE. Все права защищены.</p>
        </div>
      </div>
    </div>
  </footer>

  <style>
    /* EXACT COPY OF blondeplace.ru STYLING */
    :root {
      --primary-color: #d4a574;
      --secondary-color: #e6c4a8;
      --background-color: #faf9f7;
      --text-color: #2a2a2a;
      --light-text: #666;
      --border-color: #e8e5e0;
      --card-bg: #ffffff;
      --gold-gradient: linear-gradient(135deg, #f5d5b8 0%, #e6c4a8 100%);
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Inter', sans-serif;
      background-color: var(--background-color);
      color: var(--text-color);
      line-height: 1.6;
      font-size: 16px;
    }

    /* HEADER - EXACT COPY */
    .main-header {
      background: var(--background-color);
      padding: 20px 0;
      border-bottom: 1px solid var(--border-color);
      position: sticky;
      top: 0;
      z-index: 100;
    }

    .header-container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 0 20px;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .logo {
      display: flex;
      align-items: center;
    }

    .logo-text {
      font-family: 'Montserrat', sans-serif;
      font-size: 24px;
      font-weight: 600;
      color: var(--primary-color);
      letter-spacing: 1px;
    }

    .header-buttons {
      display: flex;
      align-items: center;
      gap: 12px;
    }

    .header-btn {
      width: 44px;
      height: 44px;
      border: none;
      background: var(--gold-gradient);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      color: white;
      cursor: pointer;
      transition: all 0.3s ease;
      box-shadow: 0 2px 8px rgba(212, 165, 116, 0.3);
    }

    .header-btn:hover {
      transform: translateY(-2px);
      box-shadow: 0 4px 12px rgba(212, 165, 116, 0.4);
    }

    .header-menu-btn {
      width: 44px;
      height: 44px;
      border: none;
      background: var(--gold-gradient);
      border-radius: 8px;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      gap: 4px;
      cursor: pointer;
      transition: all 0.3s ease;
      box-shadow: 0 2px 8px rgba(212, 165, 116, 0.3);
    }

    .header-menu-btn:hover {
      transform: translateY(-2px);
      box-shadow: 0 4px 12px rgba(212, 165, 116, 0.4);
    }

    .header-menu-btn span {
      width: 20px;
      height: 2px;
      background: white;
      border-radius: 1px;
    }

    /* TYPOGRAPHY */
    h1, h2, h3, h4, h5, h6 {
      font-family: 'Montserrat', sans-serif;
      font-weight: 600;
      color: var(--text-color);
    }

    h1 {
      font-size: 3.5rem;
      line-height: 1.2;
    }

    h2 {
      font-size: 2.5rem;
      line-height: 1.3;
    }

    h3 {
      font-size: 1.8rem;
      line-height: 1.4;
    }

    p {
      font-size: 1rem;
      line-height: 1.7;
      color: var(--light-text);
    }

    a {
      color: var(--primary-color);
      text-decoration: none;
      transition: color 0.3s ease;
    }

    a:hover {
      color: var(--secondary-color);
    }

    /* BUTTONS */
    .btn {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      padding: 14px 28px;
      border: none;
      border-radius: 8px;
      font-family: 'Inter', sans-serif;
      font-size: 1rem;
      font-weight: 500;
      text-decoration: none;
      cursor: pointer;
      transition: all 0.3s ease;
      white-space: nowrap;
    }

    .btn-primary {
      background: var(--gold-gradient);
      color: white;
      box-shadow: 0 4px 12px rgba(212, 165, 116, 0.3);
    }

    .btn-primary:hover {
      transform: translateY(-2px);
      box-shadow: 0 6px 16px rgba(212, 165, 116, 0.4);
      color: white;
    }

    .btn-secondary {
      background: transparent;
      color: var(--primary-color);
      border: 2px solid var(--primary-color);
    }

    .btn-secondary:hover {
      background: var(--primary-color);
      color: white;
    }

    /* CARDS */
    .card {
      background: var(--card-bg);
      border-radius: 12px;
      padding: 24px;
      box-shadow: 0 4px 20px rgba(0,0,0,0.08);
      border: 1px solid var(--border-color);
      transition: all 0.3s ease;
    }

    .card:hover {
      transform: translateY(-4px);
      box-shadow: 0 8px 30px rgba(0,0,0,0.12);
    }

    /* FOOTER */
    .main-footer {
      background: var(--card-bg);
      padding: 60px 0 30px;
      border-top: 1px solid var(--border-color);
      margin-top: 80px;
    }

    .footer-container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 0 20px;
    }

    .footer-sections {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 40px;
      margin-bottom: 40px;
    }

    .footer-section h3 {
      font-size: 1.2rem;
      margin-bottom: 20px;
      color: var(--text-color);
    }

    .footer-section ul {
      list-style: none;
    }

    .footer-section ul li {
      margin-bottom: 12px;
    }

    .footer-section ul li a {
      color: var(--light-text);
      font-size: 0.95rem;
    }

    .footer-logo-text {
      font-family: 'Montserrat', sans-serif;
      font-size: 1.8rem;
      font-weight: 600;
      color: var(--primary-color);
      margin-bottom: 8px;
    }

    .footer-tagline {
      color: var(--light-text);
      font-size: 0.9rem;
    }

    .contact-info p {
      margin-bottom: 8px;
      color: var(--light-text);
    }

    .contact-info strong {
      color: var(--text-color);
    }

    .metro-info {
      font-size: 0.85rem;
      line-height: 1.4;
    }

    .phone-input-wrapper {
      display: flex;
      margin-top: 12px;
    }

    .phone-input {
      flex: 1;
      padding: 8px 12px;
      border: 1px solid var(--border-color);
      border-radius: 6px 0 0 6px;
      font-size: 0.9rem;
    }

    .phone-submit {
      padding: 8px 12px;
      background: var(--primary-color);
      color: white;
      border: none;
      border-radius: 0 6px 6px 0;
      cursor: pointer;
    }

    .footer-bottom {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding-top: 30px;
      border-top: 1px solid var(--border-color);
      flex-wrap: wrap;
      gap: 20px;
    }

    .footer-legal p,
    .footer-copyright p {
      font-size: 0.85rem;
      color: var(--light-text);
    }

    /* RESPONSIVE */
    @media (max-width: 768px) {
      .header-container {
        padding: 0 15px;
      }

      .logo-text {
        font-size: 20px;
      }

      .header-buttons {
        gap: 8px;
      }

      .header-btn,
      .header-menu-btn {
        width: 40px;
        height: 40px;
      }

      .footer-sections {
        grid-template-columns: 1fr;
        gap: 30px;
      }

      .footer-bottom {
        flex-direction: column;
        text-align: center;
      }

      h1 {
        font-size: 2.5rem;
      }

      h2 {
        font-size: 2rem;
      }
    }

    /* SCROLLBAR */
    ::-webkit-scrollbar {
      width: 8px;
    }

    ::-webkit-scrollbar-track {
      background: var(--background-color);
    }

    ::-webkit-scrollbar-thumb {
      background: var(--primary-color);
      border-radius: 4px;
    }

    ::-webkit-scrollbar-thumb:hover {
      background: var(--secondary-color);
    }
  </style>
</body>
</html>
