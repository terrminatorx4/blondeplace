---
import Layout from '../layouts/Layout.astro';
import { getCollection } from 'astro:content';

// Получаем последние статьи
const allPosts = await getCollection('posts');
const recentPosts = allPosts
  .sort((a, b) => new Date(b.data.pubDate).valueOf() - new Date(a.data.pubDate).valueOf())
  .slice(0, 6);

const pageTitle = "BlondePlace Beauty Blog | Экспертные советы по красоте";
const pageDescription = "Профессиональные советы по уходу за волосами, окрашиванию, маникюру и beauty-процедурам от экспертов салона BlondePlace";
---

<Layout title={pageTitle} description={pageDescription}>
  <div class="container">
    <!-- Hero Section -->
    <section class="hero-section">
      <div class="hero-content">
        <div class="hero-text">
          <h1 class="hero-title">
            Экспертные советы<br>
            <span class="highlight">по красоте</span>
          </h1>
          <p class="hero-description">
            Профессиональные статьи о уходе за волосами, окрашивании, маникюре 
            и beauty-процедурах от мастеров салона BlondePlace
          </p>
          <div class="hero-cta">
            <a href="/blog" class="cta-primary">
              Читать блог
            </a>
            <a href="/kontakty" class="cta-secondary">
              Записаться в салон
            </a>
          </div>
        </div>
        <div class="hero-image">
          <div class="hero-badge">
            ✨ Beauty<br>Эксперты
          </div>
        </div>
      </div>
    </section>

    <!-- Features Section -->
    <section class="features-section">
      <h2 class="section-title">Почему наш блог?</h2>
      <div class="features-grid">
        <div class="feature-card">
          <div class="feature-icon">💄</div>
          <h3>Экспертность</h3>
          <p>Советы от профессиональных мастеров с многолетним опытом</p>
        </div>
        <div class="feature-card">
          <div class="feature-icon">💡</div>
          <h3>Практичность</h3>
          <p>Только проверенные методики и работающие советы</p>
        </div>
        <div class="feature-card">
          <div class="feature-icon">🏆</div>
          <h3>Актуальность</h3>
          <p>Современные тренды и новейшие техники красоты</p>
        </div>
        <div class="feature-card">
          <div class="feature-icon">🎯</div>
          <h3>Индивидуальность</h3>
          <p>Советы для разных типов волос, кожи и стилей</p>
        </div>
      </div>
    </section>

    <!-- Categories Section -->
    <section class="categories-section">
      <h2 class="section-title">Категории статей</h2>
      <div class="categories-grid">
        <a href="/blog?category=hair-coloring" class="category-card">
          <h3>🎨 Окрашивание волос</h3>
          <p>Техники, тренды, уход за окрашенными волосами</p>
        </a>
        <a href="/blog?category=hair-care" class="category-card">
          <h3>💆‍♀️ Уход за волосами</h3>
          <p>Профессиональные секреты здоровых волос</p>
        </a>
        <a href="/blog?category=nail-care" class="category-card">
          <h3>💅 Маникюр и педикюр</h3>
          <p>Nail-арт, уход за ногтями, новинки</p>
        </a>
        <a href="/blog?category=beauty-tips" class="category-card">
          <h3>✨ Beauty советы</h3>
          <p>Комплексный уход и секреты красоты</p>
        </a>
      </div>
    </section>

    <!-- Recent Posts Section -->
    {recentPosts.length > 0 && (
      <section class="recent-posts-section">
        <div class="section-header">
          <h2 class="section-title">Последние статьи</h2>
          <a href="/blog" class="view-all-link">Все статьи →</a>
        </div>
        <div class="posts-grid">
          {recentPosts.map((post) => (
            <article class="post-card">
              <div class="post-image">
                {post.data.heroImage ? (
                  <img src={post.data.heroImage} alt={post.data.title} loading="lazy" />
                ) : (
                  <div class="post-placeholder">
                    <span class="post-category">{post.data.category}</span>
                  </div>
                )}
              </div>
              <div class="post-content">
                <div class="post-meta">
                  <span class="post-date">
                    {new Date(post.data.pubDate).toLocaleDateString('ru-RU', {
                      year: 'numeric',
                      month: 'long',
                      day: 'numeric'
                    })}
                  </span>
                  <span class="post-category-tag">{post.data.category}</span>
                </div>
                <h3 class="post-title">
                  <a href={`/blog/${post.slug}/`}>{post.data.title}</a>
                </h3>
                <p class="post-excerpt">{post.data.description}</p>
                <a href={`/blog/${post.slug}/`} class="read-more">
                  Читать далее →
                </a>
              </div>
            </article>
          ))}
        </div>
      </section>
    )}

    <!-- CTA Section -->
    <section class="cta-section">
      <div class="cta-content">
        <h2>Готовы преобразиться?</h2>
        <p>Записывайтесь на консультацию к нашим мастерам и получите персональные рекомендации по уходу</p>
        <a href="/kontakty" class="cta-button-large">
          Записаться в салон BlondePlace
        </a>
      </div>
    </section>
  </div>

  <style>
    /* === HERO SECTION === */
    .hero-section {
      padding: 4rem 0;
      background: linear-gradient(135deg, var(--blonde-gold), var(--blonde-rose));
      border-radius: 20px;
      margin-bottom: 4rem;
    }

    .hero-content {
      display: grid;
      grid-template-columns: 1fr 300px;
      gap: 3rem;
      align-items: center;
      padding: 0 2rem;
    }

    .hero-title {
      font-family: var(--font-heading);
      font-size: 3.5rem;
      font-weight: 700;
      line-height: 1.1;
      margin-bottom: 1.5rem;
      color: var(--soft-black);
    }

    .highlight {
      color: var(--beauty-pink);
    }

    .hero-description {
      font-size: 1.2rem;
      color: var(--elegant-gray);
      margin-bottom: 2rem;
      line-height: 1.6;
    }

    .hero-cta {
      display: flex;
      gap: 1rem;
      flex-wrap: wrap;
    }

    .cta-primary, .cta-secondary {
      padding: 1rem 2rem;
      border-radius: 25px;
      text-decoration: none;
      font-weight: 600;
      transition: all 0.3s ease;
    }

    .cta-primary {
      background: var(--beauty-pink);
      color: var(--pure-white);
    }

    .cta-secondary {
      background: var(--pure-white);
      color: var(--soft-black);
      border: 2px solid var(--beauty-pink);
    }

    .cta-primary:hover, .cta-secondary:hover {
      transform: translateY(-2px);
      box-shadow: 0 10px 25px rgba(0,0,0,0.2);
    }

    .hero-image {
      position: relative;
      height: 300px;
      background: linear-gradient(45deg, var(--salon-purple), var(--beauty-pink));
      border-radius: 20px;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .hero-badge {
      background: var(--pure-white);
      padding: 1.5rem;
      border-radius: 15px;
      text-align: center;
      font-weight: 600;
      color: var(--soft-black);
      box-shadow: 0 5px 20px rgba(0,0,0,0.1);
    }

    /* === SECTIONS === */
    .section-title {
      font-family: var(--font-heading);
      font-size: 2.5rem;
      text-align: center;
      margin-bottom: 3rem;
      color: var(--soft-black);
    }

    .features-section, .categories-section, .recent-posts-section {
      margin-bottom: 4rem;
    }

    /* === FEATURES GRID === */
    .features-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 2rem;
    }

    .feature-card {
      text-align: center;
      padding: 2rem;
      background: var(--pure-white);
      border-radius: 15px;
      box-shadow: 0 5px 20px rgba(0,0,0,0.05);
      transition: transform 0.3s ease;
    }

    .feature-card:hover {
      transform: translateY(-5px);
    }

    .feature-icon {
      font-size: 3rem;
      margin-bottom: 1rem;
    }

    .feature-card h3 {
      font-family: var(--font-heading);
      font-size: 1.5rem;
      margin-bottom: 1rem;
      color: var(--soft-black);
    }

    /* === CATEGORIES GRID === */
    .categories-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: 1.5rem;
    }

    .category-card {
      padding: 2rem;
      background: linear-gradient(135deg, var(--warm-beige), var(--pure-white));
      border-radius: 15px;
      text-decoration: none;
      color: var(--soft-black);
      transition: all 0.3s ease;
      border: 1px solid rgba(0,0,0,0.05);
    }

    .category-card:hover {
      transform: translateY(-3px);
      box-shadow: 0 10px 30px rgba(0,0,0,0.1);
    }

    .category-card h3 {
      font-family: var(--font-heading);
      font-size: 1.3rem;
      margin-bottom: 0.5rem;
    }

    /* === POSTS GRID === */
    .section-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 2rem;
    }

    .view-all-link {
      color: var(--beauty-pink);
      text-decoration: none;
      font-weight: 500;
    }

    .posts-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
      gap: 2rem;
    }

    .post-card {
      background: var(--pure-white);
      border-radius: 15px;
      overflow: hidden;
      box-shadow: 0 5px 20px rgba(0,0,0,0.05);
      transition: transform 0.3s ease;
    }

    .post-card:hover {
      transform: translateY(-5px);
    }

    .post-image {
      height: 200px;
      overflow: hidden;
      background: var(--warm-beige);
    }

    .post-image img {
      width: 100%;
      height: 100%;
      object-fit: cover;
    }

    .post-placeholder {
      height: 100%;
      display: flex;
      align-items: center;
      justify-content: center;
      background: linear-gradient(135deg, var(--blonde-gold), var(--blonde-rose));
    }

    .post-content {
      padding: 1.5rem;
    }

    .post-meta {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 1rem;
      font-size: 0.9rem;
      color: var(--elegant-gray);
    }

    .post-category-tag {
      background: var(--beauty-pink);
      color: var(--pure-white);
      padding: 0.3rem 0.8rem;
      border-radius: 12px;
      font-size: 0.8rem;
    }

    .post-title a {
      font-family: var(--font-heading);
      font-size: 1.3rem;
      color: var(--soft-black);
      text-decoration: none;
      margin-bottom: 0.5rem;
      display: block;
    }

    .post-title a:hover {
      color: var(--beauty-pink);
    }

    .post-excerpt {
      color: var(--elegant-gray);
      margin-bottom: 1rem;
      line-height: 1.5;
    }

    .read-more {
      color: var(--beauty-pink);
      text-decoration: none;
      font-weight: 500;
    }

    /* === CTA SECTION === */
    .cta-section {
      background: linear-gradient(135deg, var(--salon-purple), var(--beauty-pink));
      padding: 4rem 2rem;
      border-radius: 20px;
      text-align: center;
      margin-top: 4rem;
    }

    .cta-content h2 {
      font-family: var(--font-heading);
      font-size: 2.5rem;
      color: var(--pure-white);
      margin-bottom: 1rem;
    }

    .cta-content p {
      font-size: 1.2rem;
      color: rgba(255,255,255,0.9);
      margin-bottom: 2rem;
    }

    .cta-button-large {
      display: inline-block;
      background: var(--pure-white);
      color: var(--soft-black);
      padding: 1.2rem 3rem;
      border-radius: 25px;
      text-decoration: none;
      font-weight: 600;
      font-size: 1.1rem;
      transition: all 0.3s ease;
    }

    .cta-button-large:hover {
      transform: translateY(-3px);
      box-shadow: 0 10px 30px rgba(0,0,0,0.2);
    }

    /* === RESPONSIVE === */
    @media (max-width: 768px) {
      .hero-content {
        grid-template-columns: 1fr;
        text-align: center;
        gap: 2rem;
      }

      .hero-title {
        font-size: 2.5rem;
      }

      .hero-cta {
        justify-content: center;
      }

      .section-header {
        flex-direction: column;
        gap: 1rem;
        text-align: center;
      }

      .cta-content h2 {
        font-size: 2rem;
      }
    }
  </style>
</Layout>
