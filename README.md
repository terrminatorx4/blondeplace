# Высокопроизводительный чеккер куков Outlook

Современное решение для массовой проверки валидности куков Microsoft Outlook с поддержкой асинхронных запросов, прокси и ротации User-Agent.

## ✨ Возможности

- **🚀 Высокая производительность** - асинхронная обработка до 50+ аккаунтов одновременно
- **🔄 Поддержка прокси** - автоматическая ротация прокси серверов
- **🎭 Ротация User-Agent** - избежание блокировок
- **📊 Детальная аналитика** - статистика проверки и времени ответа
- **💾 Множественные форматы** - поддержка JSON, CSV, TXT
- **🔁 Повторные попытки** - автоматические ретраи при ошибках
- **📝 Подробное логирование** - отслеживание всех операций
- **⚙️ Гибкая конфигурация** - настройка всех параметров

## 🛠 Установка

```bash
# Клонирование репозитория
git clone <repository-url>
cd outlook-cookie-checker

# Установка зависимостей
pip install -r requirements.txt

# Или установка через pip
pip install aiohttp certifi asyncio-throttle
```

## 📝 Подготовка данных

### Формат файла с аккаунтами

Создайте файл `accounts.txt` с данными в одном из форматов:

```text
# Простой формат
user1@outlook.com:sessionid=abc123; auth_token=xyz789

# JSON формат  
user2@hotmail.com:{"sessionid": "abc123", "auth_token": "xyz789"}

# Многострочный с разделителем |
user3@live.com|sessionid=abc123
auth_token=xyz789
csrftoken=token123
```

### Файл с прокси (опционально)

Создайте файл `proxies.txt`:

```text
http://127.0.0.1:8080
http://user:pass@proxy.example.com:3128
socks5://127.0.0.1:1080
```

## 🚀 Использование

### Базовое использование

```bash
python outlook_cookie_checker.py accounts.txt -o results.json
```

### Расширенные опции

```bash
python outlook_cookie_checker.py accounts.txt \
  -o results.json \
  -c 100 \
  -t 15 \
  -r 5 \
  -d 0.05 \
  --proxy-file proxies.txt \
  --format json \
  -v
```

### Параметры командной строки

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `input_file` | Файл с аккаунтами | - |
| `-o, --output` | Файл для результатов | auto |
| `-c, --concurrent` | Макс. одновременных проверок | 50 |
| `-t, --timeout` | Таймаут запроса (сек) | 30 |
| `-r, --retries` | Повторные попытки | 3 |
| `-d, --delay` | Задержка между запросами | 0.1 |
| `--proxy-file` | Файл с прокси | - |
| `--format` | Формат вывода (json/csv/txt) | json |
| `--no-ua-rotation` | Отключить ротацию UA | false |
| `-v, --verbose` | Подробный вывод | false |

## 📊 Результаты

### Статистика в консоли

```
==================================================
СТАТИСТИКА ПРОВЕРКИ
==================================================
Всего аккаунтов: 1000
✅ Валидных: 650 (65.0%)
⚠️ Истекших: 200 (20.0%)
❌ Невалидных: 100 (10.0%)
💥 Ошибок: 50 (5.0%)
⏱️ Среднее время ответа: 1.23с
==================================================
```

### Файлы результатов

- `results.json` - полные результаты
- `results_valid.json` - только валидные аккаунты
- `outlook_checker.log` - лог файл

### Формат JSON результата

```json
[
  {
    "email": "user@outlook.com",
    "cookies": "sessionid=...",
    "status": "valid",
    "last_check": "2024-01-15 10:30:25",
    "response_time": 1.234,
    "error_message": null,
    "proxy": "http://proxy.example.com:8080",
    "user_agent": "Mozilla/5.0..."
  }
]
```

## 🔧 Статусы проверки

- **`valid`** - Куки действительны, аккаунт активен
- **`expired`** - Куки истекли, требуется переавторизация  
- **`invalid`** - Куки недействительны или аккаунт заблокирован
- **`error`** - Техническая ошибка при проверке

## ⚡ Оптимизация производительности

### Рекомендуемые настройки

```bash
# Для быстрой проверки (много ресурсов)
python outlook_cookie_checker.py accounts.txt -c 100 -d 0.05 -t 15

# Для стабильной проверки (меньше нагрузки)  
python outlook_cookie_checker.py accounts.txt -c 25 -d 0.2 -t 30

# Для проверки через прокси
python outlook_cookie_checker.py accounts.txt --proxy-file proxies.txt -c 50
```

### Факторы производительности

1. **Количество одновременных соединений** (`-c`)
   - Больше = быстрее, но выше нагрузка
   - Оптимально: 25-100

2. **Задержка между запросами** (`-d`)
   - Меньше = быстрее, выше риск блокировки
   - Оптимально: 0.05-0.5 сек

3. **Использование прокси**
   - Снижает риск блокировки IP
   - Может замедлить запросы

## 🔒 Безопасность и соответствие

### Современные требования Microsoft

- Поддержка новой аутентификации OAuth 2.0
- Совместимость с Nested App Authentication (NAA)
- Обход устаревших Exchange токенов

### Рекомендации по безопасности

1. **Используйте прокси** для избежания блокировок IP
2. **Ротируйте User-Agent** для имитации разных браузеров
3. **Настройте задержки** между запросами
4. **Мониторьте логи** на предмет блокировок

## 🛡️ Обработка ошибок

Чеккер автоматически обрабатывает:

- Таймауты соединения
- HTTP ошибки (4xx, 5xx)
- Недоступность прокси
- Неверный формат куков
- Сетевые проблемы

## 📈 Масштабирование

### Для больших объемов (10k+ аккаунтов)

1. **Разбейте на батчи**:
```bash
split -l 1000 big_accounts.txt batch_
for batch in batch_*; do
  python outlook_cookie_checker.py "$batch" -o "results_$batch.json"
done
```

2. **Используйте несколько серверов**:
```bash
# Сервер 1
python outlook_cookie_checker.py accounts_1.txt --proxy-file proxies_1.txt

# Сервер 2  
python outlook_cookie_checker.py accounts_2.txt --proxy-file proxies_2.txt
```

## 🐛 Устранение проблем

### Частые проблемы

1. **Низкий процент валидных аккаунтов**
   - Проверьте формат куков
   - Убедитесь в актуальности данных

2. **Много ошибок таймаута**
   - Увеличьте таймаут (`-t`)
   - Уменьшите конкурентность (`-c`)

3. **Блокировка IP**
   - Используйте прокси (`--proxy-file`)
   - Увеличьте задержки (`-d`)

### Отладка

```bash
# Включить подробное логирование
python outlook_cookie_checker.py accounts.txt -v

# Проверить один аккаунт
echo "test@outlook.com:cookies" | python outlook_cookie_checker.py /dev/stdin -c 1 -v
```

## 🔄 Сравнение с BAS

| Критерий | BAS | Данное решение |
|----------|-----|----------------|
| **Производительность** | Ограничена GUI | До 100+ одновременно |
| **Автоматизация** | Требует интерфейс | Полная CLI автоматизация |
| **Прокси поддержка** | Базовая | Продвинутая ротация |
| **Форматы данных** | Ограничены | JSON/CSV/TXT |
| **Современность** | Устаревает | OAuth 2.0, NAA готов |
| **Масштабируемость** | Локальная | Серверная |

## 📋 TODO / Планы развития

- [ ] Поддержка 2FA аккаунтов
- [ ] Интеграция с базами данных
- [ ] Web интерфейс для управления
- [ ] Docker контейнеризация
- [ ] Поддержка других email провайдеров
- [ ] Machine Learning для определения паттернов
- [ ] Real-time мониторинг через WebSocket

## 📄 Лицензия

MIT License - используйте свободно для коммерческих и некоммерческих целей.

## 🤝 Поддержка

При возникновении проблем:

1. Проверьте логи в `outlook_checker.log`
2. Попробуйте с меньшим количеством аккаунтов
3. Убедитесь в правильности формата данных
4. Создайте issue с подробным описанием проблемы

---

**⚡ Высокая производительность. 🔒 Современная безопасность. 📊 Детальная аналитика.**
