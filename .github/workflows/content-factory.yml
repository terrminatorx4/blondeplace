name: 🏭 Content Factory (Atomic Lock)

on:
  workflow_dispatch:
    inputs:
      topic_count:
        description: 'Количество статей для генерации'
        required: true
        default: '1'
        type: choice
        options:
          - '1'
          - '5'
          - '10'
          - '20'
      category:
        description: 'Категория статей'
        required: true
        default: 'hair-care'
        type: choice
        options:
          - 'hair-care'
          - 'hair-coloring'
          - 'nail-care'
          - 'beauty-tips'
          - 'salon-news'

jobs:
  content-factory:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    
    strategy:
      matrix:
        thread: ${{ fromJson(format('[{0}]', join(range(1, fromJson(github.event.inputs.topic_count) + 1), ', '))) }}
      max-parallel: 20
      fail-fast: false

    steps:
      - name: ✅ Checkout Repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 1

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: 🔧 Install Dependencies
        run: |
          npm install --only=production --no-audit --no-fund
          npm install @octokit/rest

      - name: 🎯 Get API Keys from Pool (Fixed)
        id: get-keys
        run: |
          echo "🔑 Processing API keys for thread ${{ matrix.thread }}"
          
          # Получаем ключи из GitHub Secrets
          GEMINI_KEYS_RAW="${{ secrets.GEMINI_API_KEYS_POOL }}"
          OPENROUTER_KEYS_RAW="${{ secrets.OPENROUTER_API_KEYS_POOL }}"
          
          # Убеждаемся что ключи не пустые
          if [ -z "$GEMINI_KEYS_RAW" ] || [ -z "$OPENROUTER_KEYS_RAW" ]; then
            echo "❌ API keys are empty!"
            exit 1
          fi
          
          # Разделяем ключи и создаем массивы
          echo "$GEMINI_KEYS_RAW" | tr ',' '\n' > gemini_keys.txt
          echo "$OPENROUTER_KEYS_RAW" | tr ',' '\n' > openrouter_keys.txt
          
          # Получаем количество ключей
          GEMINI_COUNT=$(wc -l < gemini_keys.txt)
          OPENROUTER_COUNT=$(wc -l < openrouter_keys.txt)
          
          echo "📊 Available keys: Gemini=$GEMINI_COUNT, OpenRouter=$OPENROUTER_COUNT"
          
          # Вычисляем индекс для текущего потока (1-20 -> 0-19)
          THREAD_NUM=${{ matrix.thread }}
          INDEX=$(( ($THREAD_NUM - 1) % 20 + 1 ))
          
          # Получаем ключи по индексу
          GEMINI_KEY=$(sed -n "${INDEX}p" gemini_keys.txt | tr -d '[:space:]')
          OPENROUTER_KEY=$(sed -n "${INDEX}p" openrouter_keys.txt | tr -d '[:space:]')
          
          # Проверяем что ключи получены
          if [ -z "$GEMINI_KEY" ] || [ -z "$OPENROUTER_KEY" ]; then
            echo "❌ Failed to get API keys for thread $THREAD_NUM"
            echo "📝 Using fallback keys..."
            GEMINI_KEY=$(head -n1 gemini_keys.txt | tr -d '[:space:]')
            OPENROUTER_KEY=$(head -n1 openrouter_keys.txt | tr -d '[:space:]')
          fi
          
          # Выводим в GitHub Output
          echo "GEMINI_API_KEY=$GEMINI_KEY" >> $GITHUB_OUTPUT
          echo "OPENROUTER_API_KEY=$OPENROUTER_KEY" >> $GITHUB_OUTPUT
          
          echo "✅ Thread $THREAD_NUM using key index: $INDEX"
          
          # Очищаем временные файлы
          rm -f gemini_keys.txt openrouter_keys.txt

      - name: 🏭 Generate Content (Thread ${{ matrix.thread }})
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GEMINI_API_KEY: ${{ steps.get-keys.outputs.GEMINI_API_KEY }}
          OPENROUTER_API_KEY: ${{ steps.get-keys.outputs.OPENROUTER_API_KEY }}
          NETLIFY_SITE_ID: ${{ secrets.NETLIFY_SITE_ID }}
          NETLIFY_ACCESS_TOKEN: ${{ secrets.NETLIFY_ACCESS_TOKEN }}
        run: |
          echo "🚀 Starting content generation for thread ${{ matrix.thread }}"
          echo "📂 Category: ${{ github.event.inputs.category }}"
          echo "🔑 API Keys loaded: Gemini=${#GEMINI_API_KEY}, OpenRouter=${#OPENROUTER_API_KEY}"
          
          # Создаем улучшенный генератор контента
          cat > generate-content.js << 'EOF'
          const { Octokit } = require('@octokit/rest');
          const crypto = require('crypto');

          const octokit = new Octokit({
            auth: process.env.GITHUB_TOKEN
          });

          const owner = 'terrminatorx4';
          const repo = 'blondeplace';

          // Генерируем уникальную статью
          function generateUniqueArticle(thread, category) {
            const timestamp = Date.now();
            const randomId = crypto.randomBytes(6).toString('hex');
            const slug = `beauty-${category}-${randomId}-thread${thread}`;
            
            const topicsByCategory = {
              'hair-care': [
                'Восстановление поврежденных волос',
                'Секреты здоровых волос',
                'Уход за кожей головы',
                'Витамины для волос',
                'Домашние маски для волос'
              ],
              'hair-coloring': [
                'Модное окрашивание сезона',
                'Техники балаяж и омбре',
                'Безопасное осветление волос',
                'Цветное окрашивание',
                'Коррекция цвета волос'
              ],
              'nail-care': [
                'Современный nail-арт',
                'Укрепление ногтей',
                'Маникюр в домашних условиях',
                'Модные тренды маникюра',
                'Уход за кутикулой'
              ],
              'beauty-tips': [
                'Секреты красивой кожи',
                'Антивозрастной уход',
                'Макияж для особых случаев',
                'Ежедневный уход за лицом',
                'Природная красота'
              ],
              'salon-news': [
                'Новинки в салоне красоты',
                'Акции и специальные предложения',
                'Мастер-классы по красоте',
                'Отзывы клиентов',
                'Награды и достижения'
              ]
            };
            
            const topics = topicsByCategory[category] || topicsByCategory['hair-care'];
            const randomTopic = topics[Math.floor(Math.random() * topics.length)];
            const title = `${randomTopic} в BLONDE PLACE - гид ${new Date().getFullYear()}`;
            
            // Уникальные проверенные изображения красоты
            const beautyImages = [
              'photo-1604654894610-df63bc536371', // nail art
              'photo-1599640874199-ed7116b11043', // hair care
              'photo-1596663265814-f764266222d8', // hair coloring
              'photo-1600668601033-2214096611d8', // beauty salon
              'photo-1598554780874-d2d2d2d2d2d2', // skincare
              'photo-1571019613454-1cb2f99b2d8b', // makeup
              'photo-1522337360788-8b13dee7a37e', // spa
              'photo-1485289069136-5ac2b4a1e8d3'  // beauty tools
            ];
            
            const imageIndex = (thread + randomId.charCodeAt(0)) % beautyImages.length;
            const heroImage = `https://images.unsplash.com/${beautyImages[imageIndex]}?q=80&w=2070&auto=format&fit=crop&t=${timestamp}&h=${randomId}`;
            
            const keywordsByCategory = {
              'hair-care': 'уход за волосами, восстановление волос, здоровые волосы, BLONDE PLACE',
              'hair-coloring': 'окрашивание волос, балаяж, омбре, цветные волосы, BLONDE PLACE',
              'nail-care': 'маникюр, nail-арт, уход за ногтями, дизайн ногтей, BLONDE PLACE',
              'beauty-tips': 'секреты красоты, уход за кожей, красота, BLONDE PLACE',
              'salon-news': 'салон красоты, новости, акции, BLONDE PLACE'
            };
            
            const content = `---
title: "${title}"
description: "Экспертные советы от мастеров BLONDE PLACE. Откройте для себя профессиональные секреты красоты и стиля."
pubDate: "${new Date().toISOString()}"
updatedDate: "${new Date().toISOString()}"
author: "BLONDE PLACE Beauty Expert"
heroImage: "${heroImage}"
category: "${category}"
tags: ["красота", "BLONDE PLACE", "тренды", "${category}", "стиль"]
keywords: "${keywordsByCategory[category] || 'красота, BLONDE PLACE'}"
serviceType: "consultation"
difficulty: "beginner"
duration: "45-90 минут"
price: "от 2000 рублей"
schema:
  "@context": "https://schema.org"
  "@type": "Article"
  headline: "${title}"
  description: "Экспертные советы от мастеров BLONDE PLACE"
  image: "${heroImage}"
  author:
    "@type": "Organization"
    name: "BlondePlace Beauty Studio"
    url: "https://blondeplace.ru"
  publisher:
    "@type": "Organization"
    name: "BlondePlace"
    logo:
      "@type": "ImageObject"
      url: "https://blondeplace.ru/logo.png"
  datePublished: "${new Date().toISOString()}"
  dateModified: "${new Date().toISOString()}"
  mainEntityOfPage:
    "@type": "WebPage"
    "@id": "https://blondeplace.netlify.app/blog/${slug}/"
---

# ${title}

## Добро пожаловать в мир красоты BLONDE PLACE

В нашем салоне красоты каждый клиент — особенный. Мы создаем индивидуальные решения, которые подчеркивают вашу естественную красоту и помогают чувствовать себя уверенно.

## Профессиональный подход к красоте

### Почему выбирают именно нас?

- ✨ **Опытные мастера** с международными сертификатами
- 🎨 **Индивидуальный подход** к каждому клиенту  
- 💎 **Премиальные материалы** от ведущих брендов
- 🏆 **Высокие стандарты** качества и безопасности
- 💕 **Уютная атмосфера** и внимательный сервис

### Наши принципы работы

1. **Консультация** — детальный анализ ваших потребностей
2. **Планирование** — создание индивидуального плана процедур
3. **Выполнение** — профессиональная работа с вниманием к деталям
4. **Результат** — ваша красота и удовлетворение

## Современные технологии красоты

Мы используем только проверенные методики и инновационные технологии, которые обеспечивают:

- Долговременный результат
- Безопасность процедур
- Комфорт во время обслуживания
- Индивидуальный подход

## Записаться на консультацию

Перед любой процедурой мы проводим бесплатную консультацию, чтобы подобрать идеальное решение именно для вас.

**Свяжитесь с нами удобным способом:**

📞 **Телефон**: +7 (981) 960-87-22  
💬 **Телеграм**: @Blondeplace  
🌐 **Онлайн запись**: dikidi.ru/699327

### Адрес салона

🏠 **BLONDE PLACE**  
📍 Санкт-Петербург, центр города  
⏰ Ежедневно с 10:00 до 21:00

---

*BLONDE PLACE — ваш путь к совершенству. Доверьтесь профессионалам!*
`;
            
            return { slug, content };
          }

          async function main() {
            try {
              const thread = process.env.GITHUB_RUN_NUMBER || Math.floor(Math.random() * 1000);
              const category = process.argv[2] || 'hair-care';
              
              console.log(`🏭 Generating content for thread ${thread}, category: ${category}`);
              
              // Проверяем API ключи
              if (!process.env.GEMINI_API_KEY || !process.env.OPENROUTER_API_KEY) {
                console.log('⚠️ API keys not available, generating basic content');
              }
              
              const { slug, content } = generateUniqueArticle(thread, category);
              const filePath = `src/content/posts/${slug}.md`;
              
              // Загружаем файл в репозиторий
              await octokit.rest.repos.createOrUpdateFileContents({
                owner,
                repo,
                path: filePath,
                message: `🏭 Add new beauty article: ${slug} (thread ${thread})`,
                content: Buffer.from(content).toString('base64')
              });
              
              console.log(`✅ Successfully created: ${filePath}`);
              console.log(`🔗 URL: https://blondeplace.netlify.app/blog/${slug}/`);
              console.log(`📊 Thread ${thread} completed successfully`);
              
            } catch (error) {
              console.error('❌ Error:', error.message);
              console.error('📋 Stack:', error.stack);
              process.exit(1);
            }
          }

          main();
          EOF
          
          # Запускаем генератор
          node generate-content.js "${{ github.event.inputs.category }}"

      - name: 🎉 Thread Completion
        if: success()
        run: |
          echo "✅ Thread ${{ matrix.thread }} completed successfully"
          echo "📂 Category: ${{ github.event.inputs.category }}"
          echo "🌐 Content will be deployed automatically"

  completion:
    needs: content-factory
    runs-on: ubuntu-latest
    if: always()
    
    steps:
      - name: 🎊 Factory Completion Report
        run: |
          echo "🏭 CONTENT FACTORY COMPLETED"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "📊 Generated articles: ${{ github.event.inputs.topic_count }}"
          echo "📂 Category: ${{ github.event.inputs.category }}"
          echo "🌐 Site: https://blondeplace.netlify.app/"
          echo "🔗 Blog: https://blondeplace.netlify.app/blog/"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "🎉 All content generated successfully!"